imath>=0.0.2
numpy>=2.1.3
opencv-python>=4.10.0.84
OpenEXR>=3.3.2
PySide6>=6.11.0

# Optional GPU acceleration dependencies
# Only loaded when GPU device is detected (CUDA/OpenCL)
# Falls back to CPU via numpy/cv2 if unavailable
pyopencl>=0.0.2  # OpenCL backend (optional)
pycuda>=0.0.2    # CUDA backend (optional)
